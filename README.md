# 羽球收支管理系統

文心國際青年商會羽球社收支管理 SPA

## 功能特色

- 📊 **收支總覽**：即時顯示總收入、總支出、結餘
- 📋 **記錄管理**：新增、編輯、刪除收支記錄
- 🔍 **篩選搜尋**：依日期範圍、姓名快速篩選
- 📥 **CSV 匯入**：從 Excel 匯出的 CSV 批次匯入
- 📤 **CSV 匯出**：將資料匯出為 CSV 備份
- 🔐 **權限控制**：Google 登入 + 白名單管理

## 技術架構

```
前端：HTML + Tailwind CSS + Vanilla JS
後端：Firebase (Firestore + Auth)
部署：Firebase Hosting
```

## 快速開始

### 1. 本機測試（Mock 模式）

直接用瀏覽器開啟 `index.html` 即可使用測試資料。

### 2. 正式部署

#### 步驟 1：建立 Firebase 專案

1. 前往 [Firebase Console](https://console.firebase.google.com/)
2. 建立新專案或選擇現有專案
3. 啟用 **Firestore Database**
4. 啟用 **Authentication** > **Google 登入**

#### 步驟 2：設定 Firebase

編輯 `firebase-config.js`，填入您的專案設定：

```javascript
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
```

#### 步驟 3：設定管理員白名單

在 Firestore 建立文件：

```
Collection: settings
Document: allowedEmails
Fields:
  emails: ["admin@example.com", "manager@example.com"]
```

#### 步驟 4：部署

```bash
npm install -g firebase-tools
firebase login
firebase deploy
```

## 使用說明

### 一般使用者（無需登入）

| 功能 | 說明 |
|------|------|
| 查看收支 | 開啟網頁即可查看所有記錄 |
| 篩選資料 | 使用日期範圍或姓名搜尋 |
| 匯出 CSV | 點擊「匯出」下載目前篩選結果 |

### 管理員（需 Google 登入）

| 功能 | 說明 |
|------|------|
| 新增記錄 | 點擊「新增」填寫表單 |
| 編輯記錄 | 點擊列表中的編輯圖示 |
| 刪除記錄 | 點擊列表中的刪除圖示 |
| 匯入 CSV | 點擊「匯入」選擇 CSV 檔案 |

### CSV 格式

匯入的 CSV 需包含以下欄位（欄位名稱需完全相同）：

```csv
時間,地點,姓名,身分,項目,應收,已收,狀態,備註
2024/01/28,台中市,王小明,會員,場地費,300,300,已繳清,
```

| 欄位 | 必填 | 說明 |
|------|------|------|
| 時間 | 是 | 格式 YYYY/MM/DD |
| 地點 | 否 | |
| 姓名 | 是 | |
| 身分 | 否 | 會員/非會員/- |
| 項目 | 否 | |
| 應收 | 是 | 正數=收入，負數=支出 |
| 已收 | 否 | |
| 狀態 | 否 | 已繳清/已支付/未繳/- |
| 備註 | 否 | |

## 資料結構

### Firestore Collection: `accounting`

```typescript
interface AccountingRecord {
  id: string;           // 自動生成
  date: string;         // 日期 YYYY/MM/DD
  location: string;     // 地點
  name: string;         // 姓名
  identity: string;     // 身分
  item: string;         // 項目
  amountDue: number;    // 應收
  amountPaid: number;   // 已收
  status: string;       // 狀態
  note: string;         // 備註
  createdAt: Timestamp; // 建立時間
  updatedAt: Timestamp; // 更新時間
  createdBy: string;    // 建立者 email
}
```

## 檔案結構

```
badmintonSPA/
├── index.html          # 主頁面
├── firebase-config.js  # Firebase 設定（需自行填入）
├── js/
│   └── app.js          # 主程式
├── firestore.rules     # Firestore 安全規則
├── firebase.json       # Firebase 部署設定
└── docs/
    └── 功能/
        └── 羽球收支/
            ├── 設計.md       # 原始設計文件
            ├── 需求規格.md   # PRD + User Story
            ├── 開發計畫.md   # 開發階段規劃
            ├── 介面設計.md   # UI 設計規範
            └── 技術設計.md   # 技術架構文件
```

## 安全性說明

- Firestore 規則設定為**公開讀取、白名單寫入**
- 管理員需透過 Google 登入驗證
- 僅白名單內的 email 可進行資料修改
- 建議正式上線時啟用 Firebase App Check

## 授權

MIT License
